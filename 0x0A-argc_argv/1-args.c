#include <stdio.h>
/**
 * main - a function which returns an integer values
 * @argc: is a counter
 * @argv: is an array that points on string
 * Return: returns a number
 */
int main(int argc,__attribute__((unused)) char *argv[])
{
	printf("%d\n", argc);
	return (0);
}

